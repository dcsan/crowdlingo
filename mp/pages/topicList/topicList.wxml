<view>

  <view class='h1'>TopicList</view>

  <view wx:for="{{topicData}}" 
    wx:key="{{item.cname}}" 
    bindtap='goTopic'
    data-topic="{{item}}"
    class='topic-item'>
  {{item.title}}
  </view>

  <view>more...</view>
  <view class='topic-item'>
  Suggest a topic!
  </view>

</view>
